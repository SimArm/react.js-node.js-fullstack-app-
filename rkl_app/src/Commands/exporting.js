const exportData = (sorting, dateStart, timeStart, dateEnd, timeEnd) => {
    // node.js mariadb export by sorting
    console.log(sorting, dateStart, timeStart, dateEnd, timeEnd);
}

export default exportData;